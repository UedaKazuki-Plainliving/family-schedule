# 結合テスト仕様書（画面 → API → DB）

- バージョン：v0.1
- 目的：画面操作から API を叩き、実際に DB にデータが入る／出る／変わる／消えるまでを通しで確認する。
- 実装：Playwright for Java（Browser + APIRequestContext ＋ DB直接確認）

---

## 方針

- 各ケース：DB初期化 → 画面操作 → **API応答確認** → **DBレコード確認** の3段階で検証
- DB確認は `SELECT` を JDBC で直接実行
- 利用者は LocalStorage に "お母さん" を事前に設定してスケジュール画面から開始する

---

## TC-IT-01 登録の結合

| # | 画面操作 | 期待API | 期待DB | 期待画面 |
|---|---------|---------|--------|---------|
| 01-1 | +追加 → 誰が=そよ, 日付=今日, 内容="部活" → 保存 | `POST /api/schedules` 201 | `schedules` に1件（member_id=3, date=today, content="部活"） | トースト "保存しました"、今日枠そよに "部活" |
| 01-2 | +追加 → 内容空 → 保存 | APIは呼ばれない（クライアント側バリデーション） | DB変化なし | エラー表示、モーダル残る |
| 01-3 | +追加 → 内容101文字 → 保存 | `POST` 400 または クライアント側で止まる | DB変化なし | エラー表示 |

---

## TC-IT-02 編集の結合

| # | 前提 | 画面操作 | 期待API | 期待DB | 期待画面 |
|---|-----|---------|---------|--------|---------|
| 02-1 | id=1, member=そよ, content=部活 | 予定タップ → 内容を"部活（19時まで）"に変更 → 保存 | `PUT /api/schedules/1` 200 | `UPDATE schedules SET content='部活（19時まで）'` 反映 | トースト、画面反映 |
| 02-2 | 同上 | タップ → 誰が=ゆうり に変更 → 保存 | `PUT /api/schedules/1` 200 | `member_id=4` に変化 | 旧位置（そよ行）から消え、ゆうり行に出現 |

---

## TC-IT-03 削除の結合

| # | 前提 | 画面操作 | 期待API | 期待DB | 期待画面 |
|---|-----|---------|---------|--------|---------|
| 03-1 | id=2, content=サッカー教室 | 予定タップ → 削除 → 確認ダイアログ"はい" | `DELETE /api/schedules/2` 204 | `schedules` から id=2 消失 | トースト "削除しました"、セル "予定なし" |
| 03-2 | 同上 | 削除 → 確認ダイアログ"いいえ" | API呼ばれない | DB変化なし | 画面変化なし |

---

## TC-IT-04 一覧取得の結合（表示）

| # | 前提 | 画面操作 | 期待API | 期待画面 |
|---|-----|---------|---------|---------|
| 04-1 | DBに今日2件・明日1件 | スケジュール画面を開く | `GET /api/schedules?from=today&to=tomorrow` 200 | 今日枠に2件、明日枠に1件表示 |
| 04-2 | DBに予定0件 | スケジュール画面を開く | `GET` 200, `[]` | 全員 "予定なし" |

---

## TC-IT-05 フリック時の追加取得

| # | 前提 | 画面操作 | 期待API | 期待画面 |
|---|-----|---------|---------|---------|
| 05-1 | 今日 2026-04-24、DBに 04-26 の予定1件 | 左フリック1回 | `GET /api/schedules?from=2026-04-25&to=2026-04-26` | 日付見出し "25日/26日"、明日相当枠に該当予定 |

---

## 確認項目（共通）

- API レスポンスと DB 実データが一致
- 画面に表示される内容が DB の `content` と完全一致
- 400/404 が返されたときはDBが変化しない
