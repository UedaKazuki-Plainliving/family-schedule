# 画面詳細仕様書

- バージョン：v0.2（基本設計レビュー反映）
- 前提：[基本設計書](./01_basic_design.md)、[要件定義 v0.3](../requirements/requirements.md)
- 更新履歴：
  - v0.1：初版
  - v0.2：[基本設計レビュー](./review_basic_design_v0.1.md) の DS-1〜4 を反映

---

## 共通ルール

| 項目 | 値 | 由来 |
|---|---|---|
| 最低文字サイズ | 16px | NFR-04 |
| タップ領域最小 | 44×44px | NFR-04' |
| トースト表示時間 | 2.0秒 | FR-16 |
| 色 | 主色 `#2e6bb8`、削除色 `#c0392b`、背景 `#ffffff`、枠 `#e0e0e0` | FR-19 |
| フォント | system-ui, sans-serif | ― |

---

## S-01 利用者選択画面

### 目的
初回起動時に、現在の利用者を決定して LocalStorage に保存する（FR-01, FR-02, FR-20）。

### レイアウト

```
┌─────────────────────────────┐
│     家族スケジュール         │  (タイトル)
│     あなたは誰？             │
│                             │
│  ┌────────┐  ┌────────┐    │
│  │ お父さん │  │ お母さん │   │
│  └────────┘  └────────┘    │
│  ┌────────┐  ┌────────┐    │
│  │  そよ   │  │ ゆうり  │   │
│  └────────┘  └────────┘    │
│       ┌────────┐            │
│       │ いちろう │            │
│       └────────┘            │
└─────────────────────────────┘
```

### 要素

| ID | 種別 | 内容 | 動作 |
|---|---|---|---|
| `#title` | テキスト | "家族スケジュール" | ― |
| `#prompt` | テキスト | "あなたは誰？" | ― |
| `.member-btn[data-member-id]` | ボタン ×5 | 5人の名前 | タップで `POST /api/session`（ローカル処理） → LocalStorageに保存 → S-02 へ遷移 |

### バリデーション
なし。

---

## S-02 スケジュール画面（メイン）

### 目的
今日と明日の予定を人ごとに表示する。フリックで前後日移動。

### レイアウト（スマホ縦）

```
┌───────────────────────────────────────┐
│ [≡]  家族スケジュール      [今日に戻る] │   ヘッダ（56px高）
├───────────────────────────────────────┤
│              2026-04-24              │
│        今日(金)      明日(土)          │   日付ラベル（列見出し）
├───────────┬───────────────────────────┤
│ お父さん   │ 在宅         │ 出社       │
├───────────┼─────────────┼───────────┤
│ お母さん   │ 病院(夜当直)  │ 予定なし    │
├───────────┼─────────────┼───────────┤
│ そよ      │ 部活         │ 塾         │
├───────────┼─────────────┼───────────┤
│ ゆうり    │ 予定なし     │ 予定なし    │
├───────────┼─────────────┼───────────┤
│ いちろう  │ サッカー教室  │ 予定なし    │
├───────────┴─────────────┴───────────┤
│                                       │
│                           [ +追加 ]   │   FAB（右下、56×56px）
└───────────────────────────────────────┘
```

### 要素

| ID/セレクタ | 種別 | 動作 |
|---|---|---|
| `#date-today`, `#date-tomorrow` | ラベル | 表示中日付（フリックで変化） |
| `.schedule-cell[data-member-id][data-date]` | セル | 中の予定を縦に並べる。予定なしなら "予定なし" |
| `.schedule-item` | テキスト行 | **タップ**で編集フォーム S-04 を開く（FR-23：長押しでは開かない） |
| `#btn-add` | ボタン（FAB） | タップで S-03 を開く |
| `#btn-today` | ボタン | ヘッダ右。タップで表示日付を today/tomorrow に戻す |
| 画面全体 | スワイプ領域 | 左フリック → +1日、右フリック → -1日 |

### ビューポート（DS-1）

- スマホ縦画面（iPhone SE 相当：375×667px）で **ヘッダ + 日付見出し + 5行×2列のグリッドが初期ビューポート内にスクロールなしで収まる**こと。
- 各 `.schedule-cell` の最小高さ：`(667 - 56 - 40 - 56) ÷ 5 ≒ 103px` を目安（FAB領域除く）。
- セル内の予定が3件以上の場合はセル内で縦スクロール。

### 表示ロジック

- 起動時に `GET /api/schedules?from={viewDate}&to={viewDate+1}` を叩く
- 結果を「人ごと × 日付ごと」のマップに整形して描画
- フリックで `viewDate` を更新し、再取得

### バリデーション
なし（閲覧画面）。

---

## S-03 予定入力フォーム（モーダル）

### 目的
新規予定を登録する。

### レイアウト

```
┌───────────────────────────────┐
│  予定を追加                [×]│
├───────────────────────────────┤
│ 誰が？                         │
│ [お父][お母][そよ][ゆう][いち] │   ← FR-22: 5人を横並びボタン
│       ^^^^^ (選択中は色反転)   │
│                               │
│ いつ？                         │
│ [ 2026-04-24 ▾ ]               │
│                               │
│ 何を？ (100文字まで)           │
│ ┌───────────────────────────┐ │
│ │                           │ │
│ │                           │ │
│ └───────────────────────────┘ │
│ ⚠ エラー欄 (条件成立時のみ)     │
│                               │
│        [キャンセル] [ 保存 ]   │
└───────────────────────────────┘
```

### 要素

| ID | 種別 | 初期値 | 動作 |
|---|---|---|---|
| `#form-title` | ラベル | "予定を追加" | ― |
| `.who-btn[data-member-id]` | ボタン ×5 | 現在の利用者が選択状態 | タップで選択切替（FR-22, 1タップ）。**スマホ幅が狭い場合は折り返して2段表示可。いずれの配置でも1タップで切替可能であること（DS-2）** |
| `#date-input` | 日付入力 | 表示中の日付 | 変更可 |
| `#content-input` | textarea | 空 | 最大100文字 |
| `#error-msg` | エラー表示 | 非表示 | バリデーションNGで表示 |
| `#btn-save` | ボタン | ― | `POST /api/schedules` を叩く。成功時トーストを出してモーダルを閉じる。**リクエスト送信中は disabled にしラベルを "保存中..." に変更（DS-4）** |
| `#btn-cancel` | ボタン | ― | 何もせず閉じる |
| `#btn-close` (×) | ボタン | ― | キャンセルと同じ |

### バリデーション（FR-10, FR-18）

| 条件 | メッセージ |
|---|---|
| 内容が空文字 or 空白のみ | 「内容を入力してください」 |
| 内容が101文字以上 | 「内容は100文字以内で入力してください」 |
| 誰が未選択（通常起きない） | 「誰を選んでください」 |

保存成功時：トースト「保存しました」（FR-16）。

---

## S-04 予定編集フォーム（モーダル）

S-03 とレイアウトはほぼ同じ。差分のみ。

| 項目 | S-04での差分 |
|---|---|
| `#form-title` | "予定を編集" |
| 初期値 | 編集対象の現在値（誰が・日付・内容）を流し込む |
| ボタン | `[キャンセル] [削除] [保存]` の3つ |
| `#btn-delete` | **削除色 `#c0392b`**、「保存」から離した位置（左寄せ） | FR-19 |
| 動作 | `PUT /api/schedules/{id}` / `DELETE /api/schedules/{id}` |

削除押下時は S-05 を開く。

---

## S-05 削除確認ダイアログ

### レイアウト

```
┌─────────────────────────────────┐
│  『サッカー教室』を削除しますか？  │   FR-17: 対象内容を含める
│                                 │
│         [ いいえ ] [ はい ]      │
└─────────────────────────────────┘
```

### 要素

| ID | 動作 |
|---|---|
| `#confirm-text` | "『{content}』を削除しますか？" を表示。**内容が20文字を超える場合は先頭20文字＋"…" で省略（DS-3）** |
| `#btn-no` | ダイアログを閉じる。S-04 に戻る |
| `#btn-yes` | `DELETE /api/schedules/{id}` 実行。成功時トースト「削除しました」→ S-02 へ |

---

## 入力誤操作防止（FR-23）

- 予定行（`.schedule-item`）には **click のみバインド**。`contextmenu` / `touchstart` 長押しは無反応。
- ダブルタップ拡大を抑止するため `touch-action: manipulation`。
- フリック判定の閾値：**水平30px以上、垂直20px以下**。閾値未満はタップ扱い。

---

## スタイル共通

```css
html, body { font-size: 16px; }
button, .schedule-item { min-height: 44px; min-width: 44px; }
.btn-danger  { background: #c0392b; color: #fff; }
.btn-primary { background: #2e6bb8; color: #fff; }
.toast { /* 2s で自動消滅 */ }
```
